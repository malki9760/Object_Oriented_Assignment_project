public interface IWater {
    //method returned a boolean if the animal has gills or not
    boolean hasGills();
    //method returned a boolean if the animal has lays eggs or not
    boolean hasLaysEggs();
}
